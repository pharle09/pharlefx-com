# Server-only; never expose through NEXT_PUBLIC_* or RSBuild browser definitions.
DERIV_API_TOKEN=
DERIV_ENV=demo
DERIV_WS_URL=wss://ws.deriv.com/websockets/v3
# Required by the API auth adapter. Use a signed session cookie named pharlefx_session.
TRADING_AUTH_SECRET=
